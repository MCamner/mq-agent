# v1.22 Roadmap Documentation Sync Implementation Plan

## Goal

Make the public mq-agent roadmap show v1.22.0 consistently while keeping v1.21.0 as the current next focus until its remaining gates are complete.

## Execution note — 2026-07-16

The original precondition changed before execution: v1.21.0 was completed and
merged in mq-agent PR #124, and `.mq/repo-contract.json` now names v1.22.0 as
`next_focus`. Execution therefore updates the remaining public status text to
show v1.21.0 as complete and v1.22.0 as the next planned, unshipped milestone.

## Owner repo

mq-agent

## Secondary repos

none

## Architecture boundary

- mqobsidian owns context contracts, templates, generators, and published context surfaces.
- mq-agent owns planning, workflow routing, task decomposition, and agent handoff.
- mq-mcp owns execution tools, tool safety, and runtime boundaries.
- mq-hal owns status, operator summaries, release/runbook views.
- repo-signal owns publish readiness, security/readiness scoring, and repo health checks.

## Non-goals

- Do not change `VERSION`, `pyproject.toml`, tags, releases, or changelog entries.
- Do not mark v1.21.0 or v1.22.0 work complete.
- Do not change `.mq/repo-contract.json` while v1.21.0 remains the valid `next_focus`.
- Do not add commands or behavior that are not implemented.

## Approval gates

- Before file writes: implementation approval required after this plan.
- Before commit: yes.
- Before push/merge: yes.
- Before deletion/settings changes: yes.

## Test gates

- `git diff --check`
- Markdown lint for `README.md`, `ROADMAP.md`, `docs/ROADMAP.md`, and this plan when an existing local command is available.
- `rg -n 'v1\.22\.0|Inbox ranking and promotion orchestration' ROADMAP.md docs/ROADMAP.md README.md`
- Verify `.mq/repo-contract.json` still reports v1.21.0 as `next_focus`.
- Run the repository docs-consistency check if it does not rewrite files.

## Rollback

- Before commit, restore the tracked documentation with `git restore README.md docs/ROADMAP.md`; retain this plan as the review and execution record.
- After commit, revert the single documentation commit; do not rewrite shared history.

### Task 1: Synchronize the public roadmap

**Purpose:** Add the existing root-roadmap v1.22.0 milestone to the public roadmap that README links to.

**Files:**

- Modify: `docs/ROADMAP.md`
- Read-only reference: `ROADMAP.md`
- Read-only reference: `.mq/repo-contract.json`

**Steps:**

1. Add v1.22.0 to the roadmap summary table as Planned after v1.21.0.
2. Add the v1.22.0 goal, ownership boundary, and nine unchecked tasks from the root roadmap.
3. Keep v1.21.0 as Next and preserve its two unchecked tasks.
4. Run the exact documentation gates.

**Commands:**

```bash
rg -n 'v1\.21\.0|v1\.22\.0' ROADMAP.md docs/ROADMAP.md .mq/repo-contract.json
git diff --check
```

**Expected result:**
The public roadmap exposes v1.22.0 without claiming it is current or implemented.

**Commit suggestion:**
`docs(roadmap): sync v1.22 milestone`

### Task 2: Clarify README roadmap direction

**Purpose:** Make the README roadmap summary point readers to the actual current and queued milestones.

**Files:**

- Modify: `README.md`
- Read-only reference: `ROADMAP.md`
- Read-only reference: `docs/ROADMAP.md`

**Steps:**

1. Replace the generic current-direction list with a compact statement that v1.21.0 is next and v1.22.0 follows.
2. Preserve the architecture boundaries: mq-agent orchestrates, mqobsidian owns truth, mq-mcp owns review/runtime reasoning, and mqlaunch remains thin delegation.
3. Do not advertise v1.22.0 commands as shipped.
4. Run the exact documentation gates.

**Commands:**

```bash
rg -n 'v1\.21\.0|v1\.22\.0|Current direction' README.md ROADMAP.md docs/ROADMAP.md
git diff --check
```

**Expected result:**
README accurately summarizes the roadmap sequence and links to a public roadmap containing both milestones.

**Commit suggestion:**
`docs(roadmap): sync v1.22 milestone`

### Task 3: Verify documentation consistency

**Purpose:** Prove the roadmap surfaces agree without changing release truth.

**Files:**

- Read-only reference: `README.md`
- Read-only reference: `ROADMAP.md`
- Read-only reference: `docs/ROADMAP.md`
- Read-only reference: `.mq/repo-contract.json`
- Read-only reference: `VERSION`
- Read-only reference: `pyproject.toml`

**Steps:**

1. Verify v1.21.0 remains the next focus everywhere that declares current focus.
2. Verify v1.22.0 appears in both roadmap files and README describes it as queued.
3. Verify version metadata remains 1.18.0.
4. Run lint, docs consistency, and diff checks.
5. Summarize any remaining intentional release/roadmap distinction.

**Commands:**

```bash
./scripts/check-docs-consistency.sh
git diff --check
git status --short
```

**Expected result:**
Documentation is internally consistent, while the shipped package version remains unchanged at 1.18.0.

**Commit suggestion:**
`docs(roadmap): sync v1.22 milestone`
